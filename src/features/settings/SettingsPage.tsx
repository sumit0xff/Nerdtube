import { Accessibility, Command, Info, Keyboard, PlaySquare, ShieldCheck } from 'lucide-react'
import { Badge, Divider, Panel, SectionHeader } from '@/components/ui/primitives'
import { useAppStore, type Theme } from '@/store/use-app-store'
const themes: Array<{ value: Theme; label: string; note: string }> = [{ value: 'oled', label: 'OLED Black', note: 'Pure depth for low-light focus' }, { value: 'dark', label: 'Dark Gray', note: 'Balanced contrast for every day' }, { value: 'graphite', label: 'Graphite', note: 'Soft, elevated daylight dark mode' }]
const rows = [{ icon: Accessibility, title: 'Accessibility', text: 'Reduce motion, captions, and display preferences' }, { icon: Keyboard, title: 'Keyboard shortcuts', text: 'Navigate faster with a focused command surface' }, { icon: PlaySquare, title: 'Playback', text: 'Default quality, autoplay, and queue behavior' }, { icon: ShieldCheck, title: 'Privacy', text: 'Local history and personalization controls' }, { icon: Info, title: 'About NerdTube', text: 'Version, acknowledgements, and product updates' }]
export function SettingsPage() { const theme = useAppStore((state) => state.theme); const setTheme = useAppStore((state) => state.setTheme); return <div className="mx-auto max-w-4xl px-5 py-8 md:px-8 md:py-12"><p className="text-label text-[var(--accent)]">Preferences</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.035em] md:text-4xl">Settings</h1><p className="mt-3 max-w-xl text-sm leading-relaxed text-[var(--muted)]">Tune your workspace for comfortable, distraction-free learning.</p><div className="mt-10 space-y-8"><Panel><SectionHeader eyebrow="Appearance" title="Choose your atmosphere" action={<Badge>Synced locally</Badge>}/><div className="grid gap-3 sm:grid-cols-3">{themes.map((item) => <button key={item.value} onClick={() => setTheme(item.value)} className={`relative rounded-[var(--radius-lg)] border p-3 text-left transition ${theme === item.value ? 'border-[var(--accent)] bg-[var(--accent-soft)]' : 'bg-[var(--surface-raised)] hover:border-[var(--border-strong)] hover:bg-[var(--surface-hover)]'}`}><span className={`mb-5 block h-16 rounded-[10px] border ${item.value === 'oled' ? 'bg-[#060607]' : item.value === 'dark' ? 'bg-[#222227]' : 'bg-[#38393e]'}`}/><span className="block text-sm font-medium">{item.label}</span><span className="mt-1 block text-xs leading-relaxed text-[var(--muted)]">{item.note}</span>{theme === item.value && <span className="absolute right-3 top-3 grid size-5 place-items-center rounded-full bg-[var(--accent-strong)] text-xs text-white">✓</span>}</button>)}</div></Panel><Panel><SectionHeader eyebrow="Workspace" title="Your controls"/>{rows.map(({ icon: Icon, title, text }, index) => <div key={title}><button className="flex w-full items-center gap-4 rounded-[var(--radius-md)] px-1 py-3 text-left transition hover:bg-[var(--surface-hover)]"><span className="grid size-9 place-items-center rounded-[var(--radius-md)] bg-[var(--accent-soft)] text-[var(--accent)]"><Icon size={17}/></span><span className="flex-1"><span className="block text-sm font-medium">{title}</span><span className="mt-0.5 block text-xs text-[var(--muted)]">{text}</span></span><span className="text-[var(--muted)]">›</span></button>{index < rows.length - 1 && <Divider/>}</div>)}</Panel><Panel className="flex items-center gap-4"><span className="grid size-10 place-items-center rounded-[var(--radius-md)] bg-[var(--accent-soft)] text-[var(--accent)]"><Command size={18}/></span><p className="text-sm text-[var(--text-secondary)]">Press <kbd className="mx-1 rounded border bg-[var(--surface-raised)] px-1.5 py-0.5 text-xs">⌘ K</kbd> from anywhere to open the command palette.</p></Panel></div></div> }
