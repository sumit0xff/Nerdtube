import { Check } from 'lucide-react'
import { useAppStore, type Theme } from '@/store/use-app-store'
const options: { value: Theme; label: string; description: string }[] = [{ value: 'oled', label: 'OLED', description: 'True black with vivid violet details.' }, { value: 'dark', label: 'Dark', description: 'A softly elevated charcoal palette.' }, { value: 'graphite', label: 'Graphite', description: 'A lighter dark theme for daytime focus.' }]
export function SettingsPage() { const theme = useAppStore((state) => state.theme); const setTheme = useAppStore((state) => state.setTheme); return <div className="mx-auto max-w-3xl px-5 py-10 md:px-8"><p className="text-sm font-medium text-[var(--accent)]">Preferences</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Make NerdTube yours</h1><section className="mt-10"><h2 className="text-base font-semibold">Appearance</h2><p className="mt-1 text-sm text-[var(--muted)]">Choose a low-distraction theme that feels comfortable for long sessions.</p><div className="mt-5 grid gap-3 sm:grid-cols-3">{options.map((option) => <button key={option.value} onClick={() => setTheme(option.value)} className={`relative rounded-2xl border p-4 text-left transition ${theme === option.value ? 'border-[var(--accent)] bg-violet-500/10' : 'bg-[var(--surface)] hover:border-[var(--accent)]'}`}><span className={`mb-5 block h-16 rounded-xl ${option.value === 'oled' ? 'bg-[#080809]' : option.value === 'dark' ? 'bg-[#202025]' : 'bg-[#34353a]'}`}/><span className="block text-sm font-medium">{option.label}</span><span className="mt-1 block text-xs leading-relaxed text-[var(--muted)]">{option.description}</span>{theme === option.value && <Check className="absolute right-3 top-3 text-[var(--accent)]" size={17}/>}</button>)}</div></section></div> }
