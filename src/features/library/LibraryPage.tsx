import { Heart, ListVideo } from 'lucide-react'
import { VideoRow } from '@/components/video/VideoCard'
import { useAppStore } from '@/store/use-app-store'
export function LibraryPage({ kind }: { kind: 'watch-later' | 'liked' }) { const videos = useAppStore((state) => kind === 'liked' ? state.likedVideos : state.watchLater); const isLiked = kind === 'liked'; const Icon = isLiked ? Heart : ListVideo; const title = isLiked ? 'Liked videos' : 'Watch later'; return <div className="mx-auto max-w-4xl px-5 py-10 md:px-8"><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-2xl bg-violet-500/15 text-[var(--accent)]"><Icon size={20}/></span><div><h1 className="text-2xl font-semibold">{title}</h1><p className="text-sm text-[var(--muted)]">{videos.length} {videos.length === 1 ? 'video' : 'videos'}</p></div></div>{videos.length ? <div className="mt-7">{videos.map((video) => <VideoRow key={video.id} video={video}/>)}</div> : <div className="mt-8 rounded-2xl border bg-[var(--surface)] p-8 text-sm text-[var(--muted)]">Nothing here yet. Save videos you want to revisit and they’ll appear here.</div>}</div> }
