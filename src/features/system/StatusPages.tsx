import { Compass, Construction } from 'lucide-react'
import { Link, useLocation } from 'react-router-dom'
export function PendingPage() { const { pathname } = useLocation(); const title = pathname.split('/').filter(Boolean).at(-1)?.replaceAll('-', ' ') ?? 'This section'; return <div className="grid min-h-[70vh] place-items-center p-6"><div className="max-w-md text-center"><Construction className="mx-auto text-[var(--accent)]" size={34}/><h1 className="mt-5 text-2xl font-semibold capitalize">{title}</h1><p className="mt-2 text-sm leading-relaxed text-[var(--muted)]">This curated space is being prepared. The architecture is ready for its dedicated feature module.</p></div></div> }
export function NotFoundPage() { return <div className="grid min-h-screen place-items-center p-6"><div className="text-center"><Compass className="mx-auto text-[var(--accent)]" size={38}/><p className="mt-6 text-sm text-[var(--muted)]">404 · Off the learning path</p><h1 className="mt-1 text-3xl font-semibold">That page isn’t here.</h1><Link className="mt-6 inline-block rounded-xl bg-[var(--accent-strong)] px-4 py-2 text-sm text-white" to="/">Return home</Link></div></div> }
